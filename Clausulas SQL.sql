
/* Creando tablas */

Create Table DEPT( 
DEPTNO int, 
DNAME text, 
LOC text
)

/* Insertando Datos */

insert into DEPT (DEPTNO, DNAME, LOC) values (10, 'ACCOUNTING', 'NEW YORK')

SELECT ENAME, JOB, DEPTNO
FROM EMP 
WHERE JOB = 'CLERK'

SELECT ENAME, JOB, DEPTNO
FROM EMP 
WHERE ENAME = 'JAMES'

SELECT ENAME, SAL, COMM 
FROM EMP 
WHERE SAL <= COMM;

SELECT ENAME, SAL 
FROM EMP 
WHERE SAL BETWEEN 1000 AND 1500

SELECT EMPNO, ENAME, SAL, MGR 
FROM EMP 
WHERE MGR IN (7902,7566,7788)

SELECT ENAME
FROM EMP 
WHERE ENAME LIKE 'S%'

SELECT ENAME 
FROM EMP 
WHERE ENAME LIKE '_A%'

SELECT ENAME, MGR 
FROM EMP 
WHERE MGR IS NULL

SELECT EMPNO,ENAME,JOB,SAL 
FROM EMP
WHERE SAL >= 1100 AND JOB = 'CLERK'


SELECT EMPNO,ENAME,JOB,SAL 
FROM EMP
WHERE SAL >= 1100 OR JOB = 'CLERK'

SELECT ENAME,JOB 
FROM EMP 
WHERE JOB NOT IN ('CLERK','MANAGER','ANALYST')

SELECT ENAME,JOB,SAL 
FROM EMP
WHERE JOB = 'PRESIDENT' AND SAL > 1500;


SELECT ENAME,JOB,DEPTNO,HIREDATE 
FROM EMP
ORDER BY HIREDATE

SELECT ENAME,JOB,DEPTNO,HIREDATE, 
    CONVERT(DATE, HIREDATE, 110) AS HIREDATE_2
	FROM EMP 
	ORDER BY CONVERT(DATE, HIREDATE, 110) DESC
GO

SELECT EMPNO,ENAME,SAL*12 AS ANNSAL 
FROM EMP 
ORDER BY ANNSAL


SELECT ENAME,DEPTNO,SAL
FROM EMP 
ORDER BY DEPTNO, SAL DESC

SELECT EMPNO,ENAME,DEPTNO
FROM EMP 
WHERE LOWER(ENAME) = 'blake'


SELECT ENAME, 
CONCAT(ENAME,JOB) AS 'ENAME-JOB',
LEN(ENAME) AS 'LEN',
PATINDEX('%A%', ENAME) AS 'POSITION_A'
FROM EMP 
WHERE SUBSTRING(JOB,1,5) = 'SALES'

SELECT ROUND(45.923,2) AS 'ROUND_2',
ROUND(45.923,0) AS 'ROUND_0',
ROUND(45.923,-1) AS 'ROUND_-1'


SELECT ROUND(45.923,2,1) AS 'TRUNC_2',
ROUND(45.923,0,1) AS 'TRUNC_0',
ROUND(45.923,-1,1) AS 'TRUNC_-1'

SELECT ENAME,SAL,COMM, SAL % COMM
FROM EMP 
WHERE JOB = 'SALESMAN'
AND COMM > 0



SELECT ENAME, DATEDIFF( DAY, HIREDATE, SYSDATETIME()) / 7 AS WEEKS 
FROM EMP 
WHERE DEPTNO = 10


SELECT DATEDIFF (MONTH, '11-JAN-94', '01-SEP-95') AS MONTHS_BETWEEN 

SELECT DATEADD (MONTH, 6, '11-JAN-94') AS ADD_MONTHS

SELECT EOMONTH ('01-SEP-95') AS LAST_DAY

SELECT ENAME, CONVERT(VARCHAR, HIREDATE, 6) AS 'DD-MM-YY'
FROM EMP

SELECT '$'+FORMAT(SAL, '#,0') AS SALARY 
FROM EMP 
WHERE ENAME = 'SCOTT'


SELECT ENAME,SAL,COMM, (SAL*12) + ISNULL(COMM,0)
FROM EMP


SELECT JOB, SAL, 
CASE WHEN JOB = 'ANALYST' THEN SAL*1.1
     WHEN JOB = 'CLERK' THEN SAL*1.15
	 WHEN JOB = 'MANAGER' THEN SAL*1.10 
ELSE SAL 
END REVISED_SALARY 
FROM EMP;

SELECT ENAME, ISNULL(CONVERT(VARCHAR,MGR), 'NO MANAGER') AS MGR_CONVERT
FROM EMP 
WHERE MGR IS NULL 


SELECT EMP.EMPNO, EMP.ENAME, EMP.DEPTNO, DEPT.DEPTNO, DEPT.LOC 
FROM EMP JOIN DEPT 
ON EMP.DEPTNO = DEPT.DEPTNO

SELECT E.ENAME, E.SAL, S.GRADE, S.LOSAL, S.HISAL
FROM EMP AS E LEFT JOIN SALGRADE AS S 
ON E.SAL BETWEEN S.LOSAL AND S.HISAL

SELECT E.ENAME, D.DEPTNO, D.DNAME 
FROM EMP AS E RIGHT JOIN DEPT AS D 
ON E.DEPTNO = D.DEPTNO
ORDER BY E.DEPTNO


SELECT WORKER.ENAME + ' WORKS FOR ' +MANAGER.ENAME 
FROM EMP AS WORKER JOIN EMP AS MANAGER 
ON WORKER.MGR = MANAGER.EMPNO



































































